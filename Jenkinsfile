// This Jenkinsfile is used by the Jenkins infrastructure to build and release the plugin.
// See https://github.com/jenkins-infra/pipeline-library
buildPlugin(
    useContainerAgent: true,
    configurations: [
        [platform: 'linux', jdk: 11]
    ]
)
